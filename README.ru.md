# Cyclop

*[English](README.md) · Русский*

Челка MacBook как рабочий инструмент. Нативное приложение на SwiftUI/AppKit: в покое
невидимо, по наведению мыши разворачивается вниз в панель с плеером, полкой для
файлов, историей буфера обмена и ближайшими встречами.

[![build](https://github.com/Carameil/cyclop/actions/workflows/build.yml/badge.svg)](https://github.com/Carameil/cyclop/actions/workflows/build.yml)

![Панель Cyclop](docs/panel.png)

## Этот форк

Личный форк [akalikbergenov/cyclop](https://github.com/akalikbergenov/cyclop) с
несколькими добавлениями для ежедневной бэкенд-работы. Всё остальное — как в апстриме.

| Добавлено | Что делает |
|---|---|
| **Инструменты** | Слева вставляешь, справа результат. Минифицированный JSON форматируется с сохранением порядка ключей; столбик id склеивается через запятую; unix-время превращается в дату, дата — в unix-время. Клик по результату копирует его |
| **Хоткей** | `⌃⌥Space` открывает панель на «Инструментах», где бы ни был курсор, и сразу отдаёт клавиатуру. Esc на пустом поле закрывает. Комбинация — `hotkey` в `config.json`, пустая строка выключает. Универсальный доступ не нужен |
| **Поиск по буферу** | Фильтр над историей: сорок строк глазами уже не пробежать |
| **Хранение снимков** | Настройки → «Хранить только сегодняшние снимки»: всё старше уходит в Корзину при запуске и в полночь. По умолчанию выключено, как в апстриме. `screenshotRetentionDays` в `config.json` для любого другого окна. На полке появилась кнопка «Снимки в Корзину» |
| **Громкость** | Ползунок системной громкости рядом с кнопками плеера на вкладке «Музыка». Управляет устройством вывода по умолчанию; если у него громкость не регулируется, ползунка нет |
| **Календарь по дням** | Список встреч показывает один день: сначала сегодня, стрелками — по дням недели. Каждый повтор регулярной встречи — отдельная строка |
| **Настройки** | Переключатели вкладок в «Показывать в панели» свёрнуты в одну строку «Вкладки» со счётчиком «N из M» |
| **Деплой через make** | `make install` собирает приложение и кладёт в `/Applications`, `make update` перед этим подтягивает ветку — [Сборка](#сборка) |

Своих релизов у форка нет: образы на странице релизов апстрима собраны без этих
добавлений. Ставится из исходников, [как именно](#сборка). macOS 15 или новее.

```
0.0 % CPU в покое  ·  ≈40 МБ + 14 МБ helper  ·  3.7 МБ бандл  ·  одно разрешение, и то по кнопке
```

Трек на снимке играет во вкладке браузера — Cyclop читает его из самой macOS, не
требуя ни разрешений, ни настроек в браузере. Как это устроено — ниже.

## Что умеет

| Вкладка | Что делает |
|---|---|
| **Музыка** | Обложка, трек, исполнитель, скраббер с перемоткой, prev / play-pause / next. Источник — **что угодно**: плеер, вкладка браузера, любое приложение, которое видит сама macOS |
| **Полка** | Перетащи файлы в челку — они лежат там, пока не понадобятся; тянешь карточку наружу и файл уходит куда нужно. Клик выбирает карточку, ⌘-клик — несколько, и тогда перетаскивается вся группа. Снимок экрана, сделанный в буфер обмена, сохраняется файлом и попадает сюда же — в том числе снятый на айфоне, если скопировать его там |
| **Буфер** | История последних 40 копирований, клик возвращает запись в буфер обмена |
| **Заготовки** | Ручной список того, что надоело набирать: почта, телефон, адрес. Добавляются кнопкой прямо в панели, удаляются крестиком на карточке; клик кладёт строку в буфер обмена. Тот же список лежит файлом в `~/Library/Application Support/Cyclop/snippets.json` и правится в редакторе, если так удобнее |
| **Календарь** | Ближайшая встреча на неделю вперёд: сколько до неё осталось, кнопка подключения к Zoom, Meet, Teams и другим. Остальные встречи — списком, по одному дню |
| **Перевод** | Слева пишешь, справа появляется перевод — сам, офлайн, средствами macOS. Английский переводится на русский, русский на английский; направление берётся из письменности. Языковые пакеты macOS не предустанавливает, поэтому первый раз их надо скачать: Системные настройки → Основные → Язык и регион → «Языки перевода…» |
| **Валюта** | Сумма с одной стороны, другая валюта с другой; вводить можно в любое поле. Курсы — единственное, что в Cyclop приходит по сети: публичная таблица дневных курсов, раз в час, и только пока вкладка включена |
| **Телесуфлёр** | Сценарий ползёт под камерой со скоростью, которую задаёте ползунком. Чёлка — единственное место на экране, которому телесуфлёр правда подходит: читаешь вплотную к объективу, и на записи это выглядит прямым взглядом, а не чтением с экрана. Пока текст идёт, панель держится открытой — сценарий читают, не касаясь трекпада |
| **Заметки** | Временные, в правом столбце иконок: быстро записать, вернуться, удалить или унести через буфер обмена. Наведение сразу даёт каретку, пустые заметки убирают себя сами |

Панель раскрывается по наведению на челку и сворачивается, когда курсор ушел.
Вкладки тоже переключаются наведением — но только если курсор на иконке
задержался: проходящий мимо ничего не переключает.
Во время перетаскивания файлов она раскрывается сама и сразу показывает полку.
Иконка в меню-баре — переключить панель, разом скрыть содержимое вкладок,
выйти. Саму иконку можно убрать: ⌘-перетащить с бара или переключателем в
настройках; повторный запуск Cyclop возвращает её обратно.

Любую вкладку можно выключить в **Настройках → «Показывать в панели»**.
Выключенная не просто пропадает с рельса — вместе с иконкой останавливается
её фон: опрос буфера, наблюдатель календаря, процесс Now Playing, запрос
курсов. Рельс — для того, куда заглядывают на секунду между делом; редкий
режим на нём не запрещён, но живёт лишь пока тот, кому он не нужен, может
его снять.

## Требования

- macOS 15 или новее (вкладка «Перевод» работает на Translation.framework)
- Swift 6 toolchain (полный Xcode не нужен, хватает Command Line Tools)
- `make` — идёт в составе Command Line Tools

На macOS 27 с одними Command Line Tools сборка сама переключается на SDK 26.x,
который лежит рядом (`Scripts/sdk.sh`): плагин, на котором в SDK 27 держится
`@State`, ставится только с Xcode. С полным Xcode ничего не меняется.

На маках без челки приложение тоже работает: панель считает челкой область
180×24 pt по центру верхней кромки экрана.

## Сборка

```bash
git clone https://github.com/Carameil/cyclop.git
cd cyclop
make install
```

| Команда | Что делает |
|---|---|
| `make build` | `Scripts/bundle.sh`: swift build, сборка `build/Cyclop.app`, подпись — ad-hoc или сертификатом из `CODESIGN_IDENTITY` |
| `make install` | `make build`, затем закрывает запущенный Cyclop, заменяет `/Applications/Cyclop.app` и запускает его |
| `make update` | `git pull --ff-only` текущей ветки, затем `make install`. Останавливается, если ветка разошлась с удалённой |
| `make test` | `Scripts/test.sh`: `swift test`, в том числе на одних Command Line Tools |

Попробовать сборку без установки: `make build && open build/Cyclop.app`.

Иконка генерируется кодом, без графических редакторов:

```bash
swift Scripts/make-icon.swift "$PWD/Resources/AppIcon.icns"
```

## Установка

Форк ставится командой `make install` и обновляется командой `make update`, см.
[Сборка](#сборка). Версия видна в меню-баре первой строкой.

Сборка из исходников не нотаризована. На маке, где её собрали, она
открывается сразу; на любом другом первый запуск идёт через **Системные
настройки → Конфиденциальность и безопасность → «Все равно открыть»**.

### Доступ к календарю между обновлениями

По умолчанию сборка подписана ad-hoc, а ad-hoc подпись — это хеш бинарника.
macOS привязывает доступ к календарю к подписи, поэтому после каждого
`make update`, который принёс изменения в коде, Cyclop снова просит полный
доступ. Самоподписанный сертификат делает подпись одинаковой от сборки к
сборке. Настраивается один раз:

1. Связка ключей → меню «Связка ключей» → Ассистент сертификации → Создать
   сертификат: имя `Cyclop Local`, тип личности — самоподписанный корневой,
   тип сертификата — для подписи кода. По умолчанию он действует год; чтобы
   дольше, отметить переопределение настроек по умолчанию и задать срок,
   например 3650 дней. Новый сертификат — новая подпись, и доступ спросят
   ещё раз.
2. Добавить в `~/.zshrc`:
   ```bash
   export CODESIGN_IDENTITY="Cyclop Local"
   ```
3. `make install`. В первый раз macOS спросит, можно ли codesign
   пользоваться ключом, — «Разрешать всегда»; календарь спросят в последний раз.

Проверка: `codesign -d -r- /Applications/Cyclop.app` должен показать
`certificate`, а не `cdhash`. Если codesign жалуется, что сертификату нет
доверия, открыть его в Связке ключей → Доверие → Подписание кода: «Всегда
доверять».

Такая сборка подписана так же, как ad-hoc, только сертификатом: без hardened
runtime и без отметки времени — они оставлены для Developer ID
(`Scripts/bundle.sh`).

Образы апстрима, `Cyclop-<версия>.dmg` со
[страницы релизов апстрима](https://github.com/akalikbergenov/cyclop/releases),
подписаны Developer ID и нотаризованы Apple, поэтому открываются с первого
раза — но без добавлений форка. При переходе между таким образом и локальной
сборкой у приложения меняется подпись, и macOS может один раз заново спросить
доступ к календарю: разрешение привязано к ней.

### Собрать образ самому

```bash
./Scripts/dmg.sh
```

Кладет `build/Cyclop-<версия>.dmg` — приложение и ярлык `/Applications` рядом.
Номер версии берется из `Scripts/version`.

### Выпустить версию

```bash
./Scripts/release.sh
```

Собирает образ, ставит тег `v<версия>` и создает релиз на GitHub с приложенным
`.dmg`. Заметки состоят из двух частей: несколько строк, написанных руками, лежат
в `docs/releases/<версия>.md`, за ними идет список коммитов, который GitHub
собирает сам. Без рукописной части скрипт откажется работать: список коммитов
отвечает на вопрос «что изменилось в коде», а пришедший спрашивает «что мне это
даст» — и второй ответ из первого не получается никаким генератором.

Номер записан **в одном месте** — `Scripts/version`. Оттуда он идет в
`Info.plist` приложения, в имя образа и в тег, поэтому разойтись они не могут.
Поднять версию — это изменить одну строку и запустить скрипт; он откажется
работать, если в дереве есть несохраненное, если коммиты не запушены или если
такой тег уже существует.

Сам образ собирает `.github/workflows/release.yml` по пушу тега, и без секретов
Developer ID и нотаризации в репозитории он релиз не выпустит — см.
[docs/signing.md](docs/signing.md). Без них форк деплоится через `make install`.

## Разрешения

**Ни одного** — пока не откроешь календарь. Приложение не просит ни
Автоматизацию, ни Универсальный доступ, ни Запись экрана, и ничего не требует
настраивать в браузере. Позиция курсора читается через `NSEvent.mouseLocation`,
буфер — через публичный `NSPasteboard`, Now Playing — через helper (см. ниже).

Доступ к Календарю — единственное разрешение, которое Cyclop вообще запрашивает.
Оно нужно только вкладке «Календарь», и системный диалог показывается не при
запуске и не при открытии вкладки, а по явному нажатию кнопки на объясняющем
экране. Не пользуешься календарём — приложение так и остаётся без разрешений.

Файл, положенный на полку из «Загрузок», «Документов» или с «Рабочего стола», —
единственное, за что macOS спросит отдельно, и спросит в момент открытия полки, а
не при запуске. Отказ ничего не ломает: карточка остаётся, просто без превью.


Разрешения понадобятся только запасному пути, если основной когда-нибудь
перестанет работать: тогда для Apple Music и Spotify будет запрошена
Автоматизация, а медиа-клавишам — Универсальный доступ.

## Как это устроено

Почему окно именно такое, почему курсор опрашивается таймером, почему
Now Playing живёт внутри `/usr/bin/perl`, во что обходится простой —
восемнадцать записок о решениях, которых по коду не видно:
**[docs/architecture.ru.md](docs/architecture.ru.md)**.

## Ограничения

- Now Playing держится на приватном фреймворке и на том, что `/usr/bin/perl`
  остается платформенным бинарником без library validation. Apple может закрыть
  это в любом обновлении — тогда включится запасной путь с Music и Spotify. По
  той же причине приложение непригодно для App Store.
- Скриптовые рантаймы (включая perl) Apple объявила устаревшими и когда-нибудь
  уберет из системы. Helper переживет это ровно до того момента.
- Полка ссылается на файлы, а не копирует их: если переместить оригинал, карточка
  исчезнет при следующем запуске. Исключение — снимки экрана из буфера обмена:
  они сохраняются в `~/Pictures/Cyclop` и не удаляются автоматически никогда,
  даже когда карточка уходит с полки. Чистит папку только пользователь: пункт
  «Очистить папку снимков» в настройках отправляет её содержимое в Корзину —
  тоже рукой, не по расписанию.
- Записи с типом `org.nspasteboard.ConcealedType` (менеджеры паролей) в историю
  буфера не попадают.
- Кнопка подключения появляется, только если ссылка на созвон есть в самом
  событии — в поле места, в заметках или в URL. Распознаются Meet, Zoom, Teams,
  Webex, Whereby, Jitsi, Телемост и Discord.
- Снимок с айфона приезжает через универсальный буфер обмена, поэтому требует
  того же, что и он: один Apple ID, включённые Bluetooth и Wi-Fi, Handoff и
  устройства рядом. И затирает буфер на маке — затёртое остаётся в истории
  «Буфера» в одном клике.
- Языки перевода macOS не предустанавливает — первый раз пакет придётся скачать
  через Системные настройки; панель говорит об этом и открывает нужный экран.

## Структура

```
Sources/Cyclop
├── main.swift                 точка входа, .accessory
├── App/
│   ├── AppDelegate.swift      иконка в меню-баре, автозапуск
│   └── Strings.swift          поиск строк в таблицах, текущий язык
├── Notch/
│   ├── NotchGeometry.swift    размеры челки и производные прямоугольники
│   ├── NotchPanel.swift       NSPanel над меню-баром
│   ├── NotchRootView.swift    hit-test панели + приемник drag&drop
│   ├── PointerWatcher.swift   опрос курсора: наведение и сквозные клики
│   ├── PanelState.swift       доля одного экрана: открыта, тянут, печатают
│   ├── NotchScreenPanel.swift панель, как она стоит на одном дисплее
│   └── NotchController.swift  одна модель, по панели на дисплей
├── Model/
│   ├── NotchViewModel.swift
│   └── PrivacyMode.swift      скрытие содержимого: разделы и раскрытия
├── Services/
│   ├── MediaController.swift  выбирает источник Now Playing
│   ├── NowPlayingFeed.swift   запуск helper в perl, разбор его stdout
│   ├── PlayerBridge.swift     запасной путь: AppleScript + медиа-клавиши
│   ├── ShelfStore.swift
│   ├── ClipboardStore.swift
│   ├── ScreenshotVault.swift  снимки из буфера на диск
│   ├── SnippetStore.swift     заготовки: чтение и запись snippets.json
│   ├── ConfigStore.swift      настройки: чтение и запись config.json
│   ├── Support.swift          ~/Library/Application Support/Cyclop
│   ├── DebouncedWrite.swift   запись на диск не чаще, чем нужно
│   ├── NoteStore.swift        временные заметки: notes.json
│   ├── Translator.swift       Translation.framework, направление по письменности
│   ├── CurrencyStore.swift    курсы по сети — единственная вкладка, которая туда ходит
│   ├── TeleprompterStore.swift сценарий и то, где остановилось чтение
│   ├── ScreenshotFolderWatcher.swift  снимки, сохранённые на диск, на полку
│   ├── CalendarStore.swift    EventKit: ближайшие встречи и ссылка на созвон
│   ├── ToolsStore.swift       текст «Инструментов»: что вставили и во что превратилось
│   ├── Normalizer.swift       JSON, столбики id, unix-время: сами преобразования
│   ├── HotkeyCenter.swift     глобальный хоткей через Carbon, без Универсального доступа
│   └── SystemVolume.swift     CoreAudio: громкость устройства вывода по умолчанию
└── UI/                        NotchShape, панели вкладок, тема

Sources/CyclopMediaHelper
└── helper.m                   dylib для /usr/bin/perl: MediaRemote -> JSON
```
